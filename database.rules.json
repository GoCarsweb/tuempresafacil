{
  "rules": {
    "tarjetas": {
      ".read": true,
      ".write": "auth != null"
    },
    "clients": {
      ".read": "auth != null",
      ".write": "auth != null"
    },
    "leads": {
      ".read": "auth != null",
      "$leadId": {
        ".write": "!data.exists()",
        ".validate": "newData.hasChildren(['nombre','telefono'])"
      }
    },
    "bookings": {
      ".read": true,
      "$date": {
        "$slot": {
          ".write": "auth != null || !data.exists()",
          ".validate": "newData.hasChildren(['nombre','telefono'])"
        }
      }
    }
  }
}
